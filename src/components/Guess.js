import React from "react";

const subtractGuessesRemaining = () => {
  guessRemaining -= 1;
};

const addGuessInput = guess => {
  guesses.push(guess);
};

export default function Guess() {
  return <div></div>;
}
